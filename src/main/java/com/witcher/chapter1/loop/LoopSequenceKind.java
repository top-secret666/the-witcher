package main.java.com.witcher.chapter1.loop;

/** Вариант перехода карта → босс. Старый EYELID_WAKE сохранён для других осколков. */
public enum LoopSequenceKind {
  /** loop_wake.gif + веки (POV пробуждение). */
  EYELID_WAKE,
  /** Процедурная ходьба сквозь ветки + Ken Burns по туманному лесу (Волк). */
  FOREST_WALK
}
